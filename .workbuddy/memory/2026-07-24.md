## 修复关卡几何卡死问题（v0.91）
- 问题反馈：用户截图显示清场后门在右上角顶层，玩家在最底层跳不上去（雪球已用完）。
- 根因：原平台落差（最高 98px+）与水平间隙把普通跳跃推到极限，雪球弹跳成了唯一路径，雪球丢失则卡死。
- 修复：
  - 关1/关2 改为楼梯式平台，落差控制在 ~60px，门可逐层普通跳跃到达。
  - 关3 Boss 限制在中央平台（`boss.plat` 边界/落地），避免掉下去后玩家够不到。
  - 更新 `snowbros-GDD.md` 版本记录与关卡几何约束。
- 校验：脚本语法 OK(24926字符)，`public/` 同步，curl 200 通过。


## 复刻《雪人兄弟 Snow Bros》(FC) — 单文件 HTML5 Canvas
- 新增 `snowbros.html`（根目录 + `public/`，已同步）与 `snowbros-GDD.md`。
- 玩法：单人/双人菜单（蓝 Nick / 红 Tom，各 3 命）；核心机制=喷雪冻结(FREEZE_FULL=100) → 推/滚雪球连杀(combo) → 踩雪球弹跳上高台。
- 平台 one-way（按住下可穿落）；雪球静止约 4s(240帧)融化复活，推下屏则销毁不复活（清场安全阀）。
- 道具：敌变球 70% 概率生成（速/冻/范/分），持续 480 帧，各自玩家独立 buff。
- 3 关纵切片：关1(4敌教学) / 关2(6敌) / 关3 Boss(HP=10，冻成球态时踩顶扣血)。
- 键位：P1 WASD+J(喷雪)+K(跳)；P2 方向键+. (喷雪)+/ (跳)。
- 接功能页：`src/views/feature/index.vue` 的 `items` 加雪人兄弟条目(⛄, external link)。
- 校验：脚本 `new Function` 语法 OK(24.7k字符)；`curl localhost:5173/front-end-navigation-bar/snowbros.html` 与 `/feature` 均 200。
- 已知笔误已修：`drawBtn` 描边色 `#556camb`→`#556cae`。

## 设计备注
- 雪人兄弟数值均为初始假设(标 [PLACEHOLDER])，待 playtest：融化时间/道具概率/持续/Boss HP 最可能需要调。
- 崩溃定义(GDD)：单关死亡>2命或卡关3分钟=太难；combo随便上10且无死亡=太易。

## 复刻《75 宾果 75 Bingo》(Sachen NES) — DOM 游戏(jQuery + oat + jquery-ui)
- 新增 `bingo75.html`(+`public/`)、`public/css/bingo75.css`、`public/js/bingo75.js`、`bingo75-GDD.md`(+`public/`)。
- 用户要求：用 `public/` 下的 css 与 jquery + jquery-ui.css，以及 `https://oat.ink/` 的 css 样式与组件。
- 技术要点（可复用坑）：
  - 从 oat.ink 抓取真实资源：`/oat.min.css`(31KB)、`/oat.min.js`(10KB) 放进 `public/css`、`public/js`。oat 用 `light-dark()` 变量 → 在 bingo75.css 设 `:root{color-scheme:dark}` 即强制暗色主题。
  - `public/css/jquery-ui.css` 默认 smoothness 主题引用 `images/ui-*.png`（11 张，code.jquery.com/ui/1.13.2/themes/smoothness/images/）。**不下载则对话框缺图破图** → 已下载到 `public/css/images/`，jquery-ui 对话框才正常。
  - 但 public 原本没有 `jquery-ui.min.js`，已补 `public/js/jquery-ui.min.js`(1.13.2) 以驱动对话框控件。
  - 复用 `public/js/game-audio-helper.js` 的 `RetroGameAudio.playTone(freq,type,dur,gain)` 做音效；`oat.min.js` 提供 `ot.toast(msg,title,{variant})`。
- 玩法：标题对话框选 1–4 张牌(每张下注10)；点 DRAW 摇 1–75 球，所有牌同号自动标记；连线(行/列/斜/四角/满卡)派彩；连击倍率(上限×5)；本金攒到 400 胜，不足下注额败。支持自动取号、随时结算收手、空格抽号、M 静音。
- 接功能页：`feature/index.vue` 的 `items` 加「75 宾果」条目(🎯, external link `bingo75.html`)。
- 校验：JS `new Function` 语法 OK(14k字符)；所有 css/js/images/feature 在 dev server 下 curl 均 200。
- 数值均为 [PLACEHOLDER] 假设，待试玩调（见 GDD 平衡表）。

## 复刻《激龟快打 TMNT》(科乐美 NES FTG) — Canvas 格斗 + oat UI
- 新增 `tmnt.html`(+`public/`)、`public/css/tmnt.css`、`public/js/tmnt.js`、`tmnt-GDD.md`(+`public/`)。
- 用户要求同 75 宾果：用 `public/` 的 jquery + jquery-ui.css + oat.ink 的 css/组件；**并且要求 UI 全部用 oat、双端(PC+H5)、移动端展示方向图标**。
- 技术要点：
  - 战斗用 Canvas(`#stage` 800x450)，UI 外壳全走 oat：标题/选人/暂停/结算用 jQuery UI 对话框(`oat-dialog` 类 + 暗色皮肤覆盖)，HUD 血条/回合点/计时/连击用 oat 风格 DOM，选人卡片用 oat card 网格。
  - 移动端：触屏检测(`ontouchstart`/`maxTouchPoints`/`pointer:coarse`)→ `body.touch` 显示 `#touchControls`(dpad ▲▼◀▶ + 拳/踢/必杀/投 按钮)，全部 oat 风格；`touch-action:none` 防误触；viewport `user-scalable=no`。
  - 输入抽象统一：键盘 held + 触摸 held + 攻击脉冲 写入同一 `InputState`，战斗逻辑无感知；双击前冲刺、按住"后"防御对双端一致。
  - 复用 `RetroGameAudio` 音效 + `ot.toast` 反馈。
- 玩法：4 龟(李奥/米开/多纳/拉斐)差异化(速度/力量/体力/必杀)；拳/踢/必杀投射/投技(破防)/防御；连击倍率(上限×3)；回合制(先胜2回合)，99秒计时，CPU AI；最高分存 localStorage `tmnt_hi`。
- 接功能页：`feature/index.vue` `items` 加「激龟快打 TMNT」条目(🐢, external link `tmnt.html`)。
- 校验：JS `new Function` 语法 OK(24789字符)；tmnt.html/css/js、共享 oat/jquery 资源、feature 均 curl 200。
- 数值均为 [PLACEHOLDER] 假设，待试玩调（见 tmnt-GDD.md 平衡表）。崩溃定义：连 3 场 0 回合胜=CPU太凶；闭眼连胜=AI太弱；命中无反馈=打击感缺失(最高优先)。
- 可复用：oat+jquery-ui 对话框暗色皮肤写法(`.ui-dialog` 覆盖 + `open` 回调隐藏关闭按钮)；`color-scheme:dark` 触发 oat 暗色；双端虚拟键 pattern 可套用到后续所有 H5 游戏。

## 修复 tmnt 标题对话框"点击开始游戏没反应"
- 用户反馈：截图显示标题对话框打开，但点击「开始游戏」无反应，画布区域全黑。
- 排查：Playwright 浏览器本地安装异常无法复刻；静态语法 OK，定位到可能是 jQuery UI 对话框重复初始化/显隐处理导致 `closeDialog→openDialog` 链失败（标题未关闭，选人对话框被压在下方）。
- 修复：
  - `public/js/tmnt.js`：重构 `openDialog/closeDialog`，改为 ready 时一次性 `initDialog`(autoOpen:false) 四个对话框，后续只调用 `dialog('open')`/`dialog('close')`。
  - 增加 `console.log` 标记关键步骤，方便用户如仍有问题可直接看控制台。
  - `public/css/tmnt.css`：调整 `.ui-widget-overlay` 为半透明暗色(55%)，让后方舞台可见；加 `.ui-dialog` 居中样式。
- 校验：JS 语法 OK(25096字符)；tmnt.html/css/js curl 200。
- 若仍异常，需要用户提供浏览器控制台的红色报错。

## 文档与部署同步（README 更新 + 一键部署）
- 用户要求：更新相关 md、git 提交、同步在线预览。
- 更新 `README.md`：在「在线预览 / 功能特性 / 近期更新 / 内置小游戏」补充 MapCN 地图展厅、AntV 示例展厅(F2/G6/L7/S2)、动态表单设计器 DyForm、经典 FC 游戏合集(雪人兄弟/75宾果/激龟快打)。
- 将根目录较新的 `snowbros-GDD.md` 同步到 `public/`（`bingo75`/`tmnt` 的 public 版已是最新，无需改动）。
- 运行 `bash scripts/deploy_update.sh "<msg>"`：本地 `npm run build` 通过(4.72s) → 提交 `ecc8c5e`(17 文件) → `git push origin main` 触发 GitHub Actions 部署 GitHub Pages。
- 注意：本次**未纳入提交**的内容（deploy 脚本仅 add README*.md/package.json/.github/src/public/scripts）：`code/`(629 个语言定义目录)、`.trae/specs/`、`create_hello_world_files.py`、`languages.yml`、`parse_languages.py`、`parsed_languages.json`、根目录 HTML/GDD 重复副本、`.workbuddy/`。如需纳入请告知。
- 在线预览：https://mhxy13867806343.github.io/front-end-navigation-bar/ ｜ Actions：https://github.com/mhxy13867806343/front-end-navigation-bar/actions
